import { createStore } from "vuex";

const store = createStore({
  state: {
    // Định nghĩa trạng thái của ứng dụng ở đây
  },
  mutations: {
    // Định nghĩa các thay đổi trạng thái ở đây
  },
  actions: {
    // Định nghĩa các hành động (actions) ở đây
  },
  getters: {
    // Định nghĩa các getters (lấy giá trị từ trạng thái) ở đây
  },
});

export default store;
